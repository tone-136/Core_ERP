Place your 256×256 PNG module icon here.
The file must be named: icon.png

The icon is referenced in sale_menus.xml as:
    web_icon="custom_sales,static/description/icon.png"

Until the real icon is added Odoo will show a default grey icon on the
home screen. The module will still install and run correctly without it.
